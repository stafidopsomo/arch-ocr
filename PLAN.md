# arch-ocr Plan

This is the working plan for rebuilding `arch-ocr` into a cloud-first packet
validation system for Greek property, architecture, and building/legal
documents.

## North Star

The product is not simply OCR.

The real workflow is:

```text
document packet
-> page-level evidence extraction
-> entity clustering
-> cross-check validation
-> structured result JSON
-> optional human report
```

Users may upload 30 to 40 related pages for the same client, office, address,
architect, engineer, owner, property, or legal/building case. The system should
read every page, extract important printed/handwritten/stamped/signed values,
preserve nearby text, group repeated values, flag variations, and produce a
validation result based only on page evidence.

The final report does not need to be generated by this app at first. The most
important deliverable is reliable structured JSON that can feed another AI
system, a UI, or a future report generator.

## Product Layers

The project should be built in two layers.

### Backend Processing Engine

This is the current focus.

Responsibilities:

- local page triage
- cloud extraction
- evidence JSON
- packet clustering
- validation checks
- cost tracking
- Railway worker/API later

The backend should not depend on a specific frontend.

### Web App Layer

This comes later.

Likely responsibilities:

- account login
- upload packet
- job status
- past reports/results database
- report viewer
- export JSON/report

The web app should keep past results and reports, but not necessarily original
input files forever. Uploaded source files can be deleted after processing or
after a short retention window to control storage cost and privacy risk.

## Current Baseline

Implemented:

- PDF page rendering to PNG via PyMuPDF.
- image input support.
- Gemini provider for cheap/free-tier vision LLM extraction.
- Google Vision provider for OCR baseline.
- OpenRouter provider for hosted model experiments.
- markdown report mode.
- `--json-mode` extraction mode.
- normalized `arch_ocr.extraction.v1` JSON artifacts.
- extraction artifacts include local page triage and source/page references.
- model JSON is schema-validated before it is accepted.
- raw provider JSON output for debugging.
- local packet/page triage command with no cloud API calls.
- private packet fixture convention under `test_inputs/private/`.

Removed from the active path:

- local Tesseract/EasyOCR/PaddleOCR/docTR experiments.
- local Ollama assumptions.
- stale FastAPI webapp.
- old multi-engine PDF report workflow.

## Setup

Create and activate the Python environment:

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
cp .env.example .env
```

Recommended cheap provider:

```env
CLOUD_PROVIDER=gemini
GEMINI_API_KEY=your_key_here
GEMINI_MODEL=gemini-2.5-flash-lite
OCR_MODEL_FALLBACKS=gemini-2.5-flash
```

One-page markdown test:

```bash
python ocr_script.py test_inputs/example_page1.pdf \
  --provider gemini \
  --max-pages 1 \
  --output output/example_page1_gemini.md \
  --raw-output output/example_page1_gemini_raw.json
```

One-page structured extraction test:

```bash
python ocr_script.py test_inputs/example_page1.pdf \
  --provider gemini \
  --json-mode \
  --max-pages 1 \
  --output output/example_page1_gemini_extraction.json \
  --raw-output output/example_page1_gemini_extraction_raw.json
```

Local page triage, no API calls:

```bash
python ocr_script.py triage test_inputs/private/packet_001 \
  --output output/packet_001_triage.json
```

Google Vision OCR baseline:

```env
CLOUD_PROVIDER=google-vision
GOOGLE_VISION_API_KEY=your_key_here
GOOGLE_VISION_LANGUAGE_HINTS=el,en
```

```bash
python ocr_script.py test_inputs/example_page1.pdf \
  --provider google-vision \
  --output output/example_page1_google_vision.md
```

Cost discipline:

- Start with `--max-pages 1`.
- Keep `--dpi` around `130` to `170` for scanned forms.
- Save `--raw-output` while comparing providers.
- Compare Gemini extraction against Google Vision OCR before scaling up.
- Avoid full 40-page packets until the schema and prompts are stable.

## Core Principle

Never jump straight from images to a final report.

Build an evidence layer first. Every conclusion must cite the evidence records
it came from.

## Processing Pipeline

### 1. Packet Intake

Input may be:

- one multi-page PDF.
- many PDFs.
- image files.
- a local folder.
- a future Railway web upload batch.

The system should create a packet record:

```json
{
  "packet_id": "uuid",
  "source_files": [],
  "created_at": "iso timestamp",
  "page_count": 0,
  "provider_config": {}
}
```

### 2. Page Rendering

Each PDF page is rendered to an image and receives a stable identity:

```json
{
  "page_id": "uuid-or-hash",
  "source_file": "document.pdf",
  "page_number": 7,
  "image_path": "runtime path or object key",
  "dpi": 150
}
```

Every extracted field must remain traceable back to a specific page.

### 3. Page Triage

Before deciding which provider path to use, every page should be classified.

Triage metadata:

```json
{
  "page_id": "page-7",
  "source_file": "document.pdf",
  "page_number": 7,
  "embedded_text_chars": 2860,
  "image_count": 1,
  "annotation_count": 0,
  "page_kind": "born_digital | scanned | mixed | unknown",
  "needs_vision": true,
  "needs_text_layer": true,
  "possible_handwriting_or_stamp": "yes | no | unknown"
}
```

Rules:

- If a page has meaningful embedded text and few/no full-page images, it is
  probably born-digital.
- If a page has no embedded text and one or more large page images, it is
  probably scanned.
- If a page has embedded text plus images/annotations/stamps/signatures, it is
  mixed and should still get a rendered visual pass.
- Static PDF inspection cannot perfectly prove that no handwriting exists. A
  born-digital PDF can still have handwriting, stamps, or signatures layered on
  top or baked into an image.

Cheap strategy:

1. Extract the native text layer when available.
2. Render a page image anyway for mixed/unknown pages.
3. Ask a cheap vision model to classify whether handwriting, stamps, or
   signatures are visible.
4. Only run full field extraction on pages that need it.

Current implementation covers the local part of this:

```bash
python ocr_script.py triage <pdf-image-or-folder>
```

It does not call Gemini or Google Vision. It inspects each page locally with
PyMuPDF and records text-layer length, image count, annotation count, ink ratio,
page kind, and whether the page probably needs vision.

Extraction artifacts also include the relevant page triage data so every model
result can be interpreted in context.

Schema validation happens before the extraction artifact is written. The model
response must include required keys such as `schema_version`, `page_results`,
`fields`, `field_type`, `value`, `confidence`, and basic evidence fields.
Invalid responses fail with explicit JSON paths instead of silently entering the
packet pipeline.

### 4. Page-Level Evidence Extraction

Each page is processed independently first. This keeps prompts focused, costs
controlled, and debugging sane.

Current target page schema:

```json
{
  "schema_version": "page_evidence.v1",
  "packet_summary": "short summary",
  "page_results": [
    {
      "page_number": 1,
      "document_type": "building permit | declaration | plan | contract | technical memo | uncertain | other",
      "language": "el | en | mixed | uncertain",
      "printed_text_summary": "short summary",
      "fields": [
        {
          "field_id": "p1-f1",
          "field_type": "person_name | address | office | property_id | date | permit_number | engineer | architect | owner | applicant | handwritten_note | stamp | signature | technical_value | other",
          "label_text": "printed or nearby words that label the value, or uncertain",
          "value": "detected value, or uncertain",
          "normalized_value": "clean normalized form if safe, else same as value or uncertain",
          "is_handwritten": false,
          "is_stamped": false,
          "is_signature": false,
          "confidence": "high | medium | low",
          "evidence": {
            "nearby_text": "words before/after or around the value",
            "location_hint": "top right | table row | signature area | margin | uncertain"
          },
          "notes": ["uncertainty, variation, or reading notes"]
        }
      ],
      "page_warnings": []
    }
  ],
  "uncertainties": []
}
```

Handwritten values are especially important. The system must record nearby
printed words before/after the handwritten value so we know whether a name was
an owner, applicant, architect, engineer, witness, or something else.

### 5. Optional OCR Baseline

Google Vision can run before or beside Gemini.

Use Google Vision for:

- raw Greek OCR text.
- printed labels around handwritten values.
- cheap quality/readability baseline.

Hybrid prompt idea:

```text
Here is the page image.
Here is baseline OCR text from Google Vision.
Use the image as the source of truth, but use OCR text to help read printed
labels and nearby context.
```

Hybrid mode should be optional. It should be used only when it measurably
improves extraction or lowers cost.

### 6. Entity Clustering

After all pages are extracted, group fields that appear to refer to the same
real-world thing.

Examples:

- same person name with spelling variations.
- same address with abbreviations or missing street number.
- same permit or registry number appearing on several pages.
- same architect/engineer referenced by printed text, stamp, and signature.

Cluster record:

```json
{
  "cluster_id": "name-1",
  "field_type": "person_name",
  "canonical_value": "Nikos Papadopoulos",
  "mentions": [
    {
      "field_id": "page-2:p2-f4",
      "value": "ΝΙΚΟΣ ΠΑΠΑΔΟΠΟΥΛΟΣ",
      "page_id": "page-2",
      "source_file": "document.pdf",
      "confidence": "high",
      "nearby_label": "Owner"
    }
  ],
  "variation_notes": []
}
```

Start simple with deterministic normalization and fuzzy string matching. Later,
use an LLM only for ambiguous merges.

### 7. Cross-Checks

The validation engine reads clusters and page evidence, then emits checks:

```json
{
  "check_id": "address-consistency",
  "status": "pass | warning | fail | unknown",
  "title": "Address consistency",
  "summary": "Address X appears 5 times, with 1 variation on page 14.",
  "evidence_refs": ["page-1:p1-f3", "page-14:p14-f8"],
  "details": []
}
```

Initial checks:

- client/person name consistency.
- address consistency.
- office/company consistency.
- architect/engineer identity.
- owner/applicant identity.
- permit/registry/property identifier consistency.
- date consistency.
- missing signatures.
- missing stamps.
- handwritten amendments.
- extra unexpected names.
- low-confidence or unreadable fields.

Example conclusion:

```text
Name A appears 10 times as owner/applicant with high confidence.
Address X appears 5 times, but page 14 contains a street-number variation.
Architect Y appears in printed text on pages 3 and 8 and as a stamp/signature
on page 22.
One additional name appears once on page 17 and needs review.
```

### 8. Final Packet Output

The final packet artifact should be machine-readable first:

```json
{
  "artifact_version": "arch_ocr.packet.v1",
  "packet_id": "uuid",
  "summary": {
    "overall_status": "pass | warning | fail | needs_review",
    "main_client": "string or uncertain",
    "main_address": "string or uncertain",
    "main_architect_or_engineer": "string or uncertain"
  },
  "source_files": [],
  "page_results": [],
  "clusters": [],
  "checks": [],
  "uncertainties": []
}
```

## Why Not Process 40 Pages In One Prompt?

One giant prompt is the wrong first design.

Problems:

- expensive.
- hard to debug.
- weak traceability.
- easy for the model to blur pages together.
- difficult to re-run only failed pages.
- harder to prove where a conclusion came from.

Better design:

- extract page by page.
- store evidence.
- cluster values.
- validate clusters.
- generate final conclusion.

## Implementation Plan

### Milestone 1: Real Document Batch Prep - Done

Goal: prepare for the documents being gathered now.

Tasks:

- Done: add `test_inputs/private/` convention for local real documents.
- Done: keep `test_inputs/private/` ignored by git.
- Done: move private test packet files to:
  - `test_inputs/private/packet_001/`
- Later: add `test_inputs/private/packet_002/` when needed.
- Later: add a short `manifest.json` per packet if needed.

Success criteria:

- Done: real documents can be dropped into a folder safely.
- Done: no private files are committed.
- Done: one-page extraction still works.

### Milestone 2: Page Extraction Hardening - Mostly Done

Goal: make page-level JSON stable enough to trust as an internal contract.

Tasks:

- Done: add validation for required keys:
  - `schema_version`
  - `page_results`
  - `fields`
  - `field_id`
  - `field_type`
  - `value`
  - `confidence`
- Done: fail clearly when provider JSON is invalid.
- Done: add field count and low-confidence count to extraction artifacts.
- Done: add `source_file`, `page_number`, `page_id`, and `field_ref` to field
  evidence records.
- Done: add page triage metadata:
  - embedded text character count
  - image count
  - page kind
  - needs vision yes/no
- Later: move schema definitions into dedicated schema module/file if the code
  grows.
- Later: capture provider token usage into normalized extraction artifacts.

Success criteria:

- Done: each accepted page extraction is valid JSON.
- Done: invalid model JSON fails clearly.
- Done: every field can be traced to page and file.
- Done: each page has enough metadata to choose text-first, vision-first, or hybrid
  processing.

### Milestone 3: Packet CLI - First Pass Done

Goal: process many files/pages and emit a single packet artifact.

Proposed command:

```bash
python ocr_script.py packet test_inputs/private/packet_001 \
  --provider gemini \
  --max-pages-per-file 5 \
  --output output/packet_001.json
```

Tasks:

- Done: add a `packet` mode.
- Done: accept a folder of PDFs/images.
- Done: render/process pages one page at a time.
- Done: include per-page extraction artifacts in packet output.
- Done: combine page results into `arch_ocr.packet.v1`.
- Add resumable output so failed pages can be retried.

Success criteria:

- a folder of documents produces one `packet.json`.
- failed pages are reported, not silently ignored.
- packet JSON includes all page evidence.

### Milestone 4: Normalization And Clustering - First Pass Done

Goal: group repeated facts across pages.

Tasks:

- Done: normalize Greek/Latin whitespace and punctuation.
- Done: normalize uppercase/lowercase and strip accents for comparisons.
- Done: normalize numeric dates to `YYYY-MM-DD`.
- Done: normalize common address punctuation and labels.
- Done: group exact normalized matches first.
- Done: preserve all mentions with `field_ref`, page/file refs, labels, nearby
  text, confidence, and handwriting/stamp/signature flags.
- Done: add local `cluster` command for existing packet JSON.
- Add fuzzy matching for near-duplicates.
- Keep all mentions even when clustered.

Success criteria:

- Done: repeated names and addresses are counted.
- Done: repeated dates, property IDs, and technical values are counted.
- Done: obvious surface-form variations are flagged inside a normalized cluster.
- Done: clusters retain evidence refs.

### Milestone 5: Validation Checks - First Pass Done

Goal: produce useful conclusions from clusters.

Current bridge toward this milestone:

- Done: add deterministic Markdown evidence report from packet JSON.
- Done: report includes processing totals, cluster summary, repeated evidence,
  identity/property evidence, errors, warnings, and manual review points.
- Done: add structured validation checks to packet JSON.
- Done: report includes validation check status, summary, evidence refs, and
  details.
- Later, after we have many more real packets: make checks more
  domain-specific and role-aware. Do not overfit these rules from the current
  small fixture set.

Tasks:

- Done: add `retry-failed` mode so failed packet pages can be retried without
  rerunning successful pages.
- Done: add checks for names, addresses, engineers, dates, identifiers,
  permit numbers, owner evidence, signatures, stamps, handwriting, extraction
  completeness, and confidence.
- Done: infer identifier subtypes for KAEK, AFM, ATAK, permit numbers, registry
  IDs, and unknown identifiers from labels and nearby evidence.
- Done: add subtype-specific checks for KAEK, AFM, ATAK, registry IDs, and
  unknown identifiers.
- Done: assign statuses:
  - `pass`
  - `warning`
  - `unknown`
- Later: add `fail` status for clear contradictions once fuzzy matching and
  role-specific types are stronger.
- Done: include evidence refs in every evidence-backed check.
- Done: produce a short summary from deterministic checks first.
- Done: add fuzzy near-match groups for address/name/role review without
  rewriting exact clusters.
- Done: promote extraction incompleteness and multi-KAEK single-property
  contradictions to `fail`; keep normal multi-document variation as `warning`.
- Done: add concise executive summary derived from checks.
- Later, with a larger corpus: add more domain-specific rules for technical
  values, permit roles, and document-type requirements.

Success criteria:

- Done: packet output can say what repeats, what varies, and what needs review.
- Done: no evidence-backed conclusion exists without evidence refs.

### Milestone 6: Cost Tracking - First Pass Done

Goal: understand cost before selling this per client.

Tasks:

- Done: record per request when provider usage metadata is available:
  - provider
  - model
  - source file
  - page number
  - input tokens
  - output tokens
  - cached input tokens when reported
  - reported provider cost when available
  - estimated cost
- Done: aggregate cost per packet in `cost_summary`.
- Done: include cost summary in Markdown reports and executive summary.
- Done: allow pricing overrides with `OCR_COST_INPUT_PER_1M_USD`,
  `OCR_COST_OUTPUT_PER_1M_USD`, and `OCR_COST_CACHED_INPUT_PER_1M_USD`.
- Later: record runtime.
- Later: add a dry-run estimator using page count and last known average token usage.
- Keep native-text pages cheaper by avoiding full vision extraction when safe.

Success criteria:

- Done: packet output includes estimated cost when usage metadata exists.
- we can estimate how many pages a 50 EUR/month client can process.
- Later: expensive pages are visible in logs.

### Milestone 7: Provider Comparison And Hybrid Mode - Deferred

Goal: decide when Google Vision helps.

Status: skip for now. Gemini is good enough for the first demo path, and Google
Vision is an optimization/fallback, not a blocker.

Tasks:

- Add a comparison command for one page.
- Run Gemini-only extraction.
- Run Google Vision OCR.
- Run Gemini-with-OCR-context extraction.
- Compare field counts, confidence, and visible errors.

Success criteria:

- we know whether hybrid mode is worth its cost on real documents.
- Google Vision remains optional.

### Milestone 8: Railway API - First Pass Done

Goal: upload and process packets in the cloud.

Railway deployment shape:

- Done: FastAPI backend in `app.py`.
- Done: upload endpoint for packets.
- Done: simple sequential background worker guarded by a process lock.
- Done: persistent file-backed storage for uploads, jobs, packet JSON, reports,
  and usage ledger under `OCR_STORAGE_DIR`.
- Done: provider API keys as Railway environment variables.
- Done: downloadable JSON and Markdown result.
- Later: database tables for users, packets, report history, billing, and admin
  analytics.

First Railway milestone:

- Done: add FastAPI demo service skeleton.
- Done: upload a packet with strict demo limits.
- Done: render pages.
- Done: process up to the packet-level page cap.
- Done: save JSON output and markdown report.
- Done: return job status.
- Done: expose cost/usage summary.
- Done: use Railway Volume-friendly file storage under `OCR_STORAGE_DIR`.
- Done: demo cookie login for `admin` and `stavret` users, configured by env
  vars.
- Done: simple token-gated admin/job list pages.
- Done: browser usage page backed by the provider usage ledger.
- Done: draft upload flow where file selection uploads first, then validation
  starts explicitly.
- Done: cooperative abort endpoint for running jobs.
- Done: dashboard deletion for stored jobs, including uploads, packet JSON,
  reports, and logs.
- Done: static prototype UI served from `/design/arch-ocr.html`.
- Done: Greek review page with validation checks, clusters, report/JSON links,
  and evidence thumbnails.
- Done: page coverage messaging for triaged, selected, extracted, and skipped
  pages so demo caps are visible.
- Done: per-job structured logs for upload, triage, page processing, provider
  retries, throttling, failures, and completion.
- Done: dynamic re-analysis for stored packets when opening packet/report/review
  so deterministic fixes can improve old jobs while source PDFs still exist.
- Done: deterministic embedded-text scan for AFM, KAEK, and ATAK identifiers.
- Later: add Postgres users/admins/accounts/subscriptions.

Temporary demo/free-tier constraints:

- Maximum uploaded files per packet: 10.
- Maximum selected pages per packet: 20.
- Maximum individual upload size: configurable env var.
- One active processing job at a time per deployment at first.
- Sequential Gemini calls; no parallel provider calls in the demo worker.
- Prefer stable Gemini model IDs for the demo. Preview models can be useful for
  experiments, but provider-capacity errors can happen even when project RPM/TPM
  usage looks low.
- Ordered model fallback is supported with `OCR_MODEL_FALLBACKS`; it is used
  only after transient provider errors and each provider event records the model
  that was actually called.
- Rate limiter controlled by env vars:
  - `OCR_MAX_FILES_PER_PACKET=10`
  - `OCR_MAX_PAGES_PER_PACKET=20`
  - `OCR_PROVIDER_MIN_SECONDS_BETWEEN_CALLS`
  - `OCR_PROVIDER_MAX_REQUESTS_PER_MINUTE`
  - `OCR_PROVIDER_MAX_REQUESTS_PER_DAY`
- Store a local usage ledger for every provider call:
  - timestamp
  - provider/model
  - input/output/total tokens
  - estimated cost
  - success/failure
- If local usage approaches configured limits, delay queued work or return a
  clear "try again later" status.

Gemini limit notes:

- Official limits are RPM, TPM, and RPD, applied per project, not per API key.
- Active limits should be checked in AI Studio because they vary by model/tier
  and can change.
- For the Railway demo, our app should be stricter than the real free-tier
  limits so a non-careful user cannot accidentally burn through the quota.
- Gemma-family models can be useful experiments, but they are not the default
  path for this product demo because the pipeline depends on PDF/image input,
  structured text output, Greek document understanding, and predictable costing.
  Keep the demo on Gemini Flash/Flash-Lite unless a focused benchmark proves a
  Gemma model handles the same pages reliably.

Keep the one-service Railway demo until it has been presented and the workflow
is accepted. Split into a proper web service + API service + Postgres only when
real users/accounts/history become necessary.

### Milestone 9: Review UI And Reporting - In Progress

Goal: make validation easy to inspect.

UI should show:

- Done: uploaded packet/job status through the static demo UI.
- Done: upload queue with per-file upload bars; selecting or dragging files starts
  upload/preflight immediately, and "Start validation" only becomes available
  after all files have finished uploading.
- Done: page evidence thumbnails in the server-rendered review page.
- Done: clear page coverage messaging for selected-vs-skipped pages.
- Later: dedicated page-by-page extracted-field browser.
- Done: clusters of repeated names/addresses/identifiers in the review page.
- Done: warnings and contradictions as validation cards with evidence refs.
- Later: raw OCR/model output view for debugging.
- Done: export JSON.
- Done: markdown/human report export.
- Done: dashboard delete action and running-job abort action.
- Done: inline job logs inside the job screen, refreshed periodically, so the
  user does not need to open a separate logs page during a live run.
- Later: replace the CDN/static prototype with a real frontend when accounts,
  persisted history, and admin workflows are ready.

Railway cost note for the demo:

- Treat network egress as a metric to watch closely. Uploaded files, generated
  packet JSON/report downloads, browser polling, CDN-loaded prototype assets,
  and any external API traffic can all contribute to visible traffic.
- If Railway egress remains high after repeated tests, prioritize reducing
  response payloads, serving minified/local UI assets, compressing large JSON
  responses, and moving any future database calls onto Railway private
  networking.

## Immediate Next Task

Stabilize **Milestone 9: Review UI And Reporting** for the friend demo.

Next implementation focus:

1. Redeploy the current Railway demo and verify an old job review shows correct
   page/field totals, AFM evidence, and thumbnails.
2. Run one fresh packet with the current cap/settings and compare the review
   page against the source PDFs.
3. Improve the review page only where it directly helps presentation:
   stronger Greek copy and page-by-page inspection where needed.
4. Keep deep domain-specific rules parked until there are many more real
   packets to investigate.
5. After the demo, decide whether the next product layer is real login/history
   or deeper extraction accuracy.
